import React from 'react';
import {Pressable, Button, View, TextInput, Text} from 'react-native';

const Schedule = (props) => {
  const schedule = props.schedule

  return (
    <View>
      <Text>Hue.</Text>
    </View>
  );
};

export default Schedule;
